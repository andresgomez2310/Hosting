server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    # Para que cualquier ruta (/, /projects, /templates, etc.)
    # devuelva siempre index.html y la maneje React Router.
    location / {
        try_files $uri /index.html;
    }
}
