# Etapa 1: build de React con Vite
FROM node:18 AS build
WORKDIR /app

# Instalar dependencias
COPY package.json package-lock.json ./
RUN npm install

# Copiar código fuente y hacer build
COPY . .
RUN npm run build

# Etapa 2: servir archivos estáticos con Nginx
FROM nginx:latest

# Copiar el build generado por Vite a la carpeta pública de nginx
COPY --from=build /app/dist/ /usr/share/nginx/html

# (Opcional pero recomendado) Config simple para SPA
# Si ya tienes nginx.conf propio, puedes mantenerlo.
# Si NO tienes, crea uno así:
# COPY nginx.conf /etc/nginx/conf.d/default.conf

COPY nginx.conf /etc/nginx/conf.d/default.conf
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]